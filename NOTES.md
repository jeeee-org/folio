# 学び・方法論（NOTES）

> 要件でも進捗でもない、**進め方・設計判断の理由・ハマりどころ・罠**を残す（メタな学び）。
> **いま効く学びだけ**を置く。項目の頭に取得日を書く（`- <YYYY-MM-DD> …`）。
> 似たトピックで開いた項目をその場で見直し、ルール・スキル・テストに昇格したら移した先を
> 1行残して消す／前提が変わって効かなくなったら消す／一回限りのログはcheckpoints/へ。

最終更新: 2026-09-12

## 依存クレートの選定理由

- 2026-09-12 TUI描画は`ratatui`（0.30）。yaziと同じ土台で、`ratatui::init()`/`restore()`で端末の初期化と復帰が1行ずつ。crosstermは`ratatui::crossterm`の再エクスポートを使い、版ずれを避ける。
- 2026-09-12 マークダウン解析は`pulldown-cmark`（0.13）。イベント列を返すプル型で軽く、表・打ち消し・タスクリストの拡張が揃う。`comrak`はAST型でGFM準拠が厚いが、描画に必要なのはイベント列で足りる。
- 2026-09-12 シンタックスハイライトは`syntect`（5.3）。Sublimeの文法で言語が広い。`tree-sitter`は言語ごとに文法を同梱ビルドする必要があり、ビューアーには重い。正規表現はC依存の`onig`でなく純Rustの`fancy-regex`（`default-fancy`）にしてビルドを単純にした（遅いが閲覧には十分）。
- 2026-09-12 syntectの配色は`base16-ocean.dark`。テーマの背景は使わず前景色だけ取り、背景はビューアーのコードブロック色（256色の236番）で塗る。前景はRGB指定なのでtruecolor非対応の端末では近似色になる。読み込みはreleaseで1ms・debugで12msなので遅延読み込みは不要。
- 2026-09-12 設定ファイルは`toml`＋`serde`（derive）。TOMLはyaziと同じで利用者に馴染みがあり、`#[serde(default, deny_unknown_fields)]`で「省略は既定・綴り間違いはエラー」が宣言だけで済む。色はratatuiの`Color::from_str`が色名・番号・`#rrggbb`を解釈するので自前のパーサ不要。
- 2026-09-12 CSV/TSVは`csv`クレート。引用符で囲んだセル・セル内の区切り文字と改行・行ごとの列数のばらつき（`flexible`）を自前で扱うと罠が多い。
- 2026-09-12 履歴は`serde_json`（1行1レコードのJSON Lines。追記だけで済み、壊れた行を飛ばせる）と`chrono`（ローカル時刻のRFC 3339。標準ライブラリにはタイムゾーンが無い）。
- 2026-09-12 CLIは`clap`（derive）。幅計算は`unicode-width`（日本語の全角を2桁として数える。折り返しの根幹）。エラーは`anyhow`。

## ターミナル/TUIの罠

- 2026-09-12 折り返し幅と描画領域を同じにすると、行頭禁則でぶら下げた句読点が領域の外に出て切れる。ビューアー側で描画領域より2桁狭く折り返す（`HANG_RESERVE`）。
- 2026-09-12 折り返し幅が変わる（目次ペインの開閉・端末のリサイズ）と行番号がずれ、画面上端の内容が変わる。スクロール位置は行番号でなく「直前の見出し＋そこからの行数」で持ち直す（`ensure_rendered`）。描画側が見出しの行位置を返す設計にした理由の1つ。
- 2026-09-12 タイトなリスト（項目間に空行なし）では、pulldown-cmarkはItemの直下にParagraphを出さずTextを直接出す。解析側で暗黙の段落を開いて吸収している。

## yaziとの連携

- 2026-09-12 `[open]`のルールのキーは`url`（`name`は今のyazi 26.xでは効かず、黙ってvimが開く）。既定の設定はバイナリに埋め込まれていて`strings /usr/local/bin/yazi | grep -A12 '^\[open\]'`で読める。
- 2026-09-12 TUIを開くopenerは`block = true`が必須（yaziが画面を明け渡して終了を待つ）。
- 2026-09-12 プレビュー欄はpiper.yaziが`$w`/`$h`を環境変数で渡し、標準出力を行単位で読んで`ui.Text.parse`でANSIを解釈する。スクロール（`job.skip`）もpiper側が行を読み飛ばして実現するので、folioは全行を吐くだけでよい。未決だった「スクロール制御の分担」はこれで解決。

## 練習モードの判断

- 2026-09-12 出題の目標位置は、模範解答をエンジンで実際に動かして求める。手で座標を計算すると、エンジン側の挙動を直した時に問題がずれる。判定が常にvimの挙動（の再現）と一致する。
- 2026-09-12 TUIの動作確認で標準出力をファイルにリダイレクトすると、描画のエスケープ列がファイルに入って画面には何も出ない。tmuxの中で動かして`capture-pane`で見る。終了まで待ってからキーを送る（早く送ると届かない）。

- 2026-09-12 使った感想: 何も知らない状態では厳格モード（間違えた瞬間に答えが出る）が助かる。「正解に辿り着く」より「答えを知る」が先に来る段階がある。既定を厳格にした判断は正しかった。

## 進め方の学び

- 2026-09-12 新しいキーを割り当てる時は、既存のvim風キー（`j/k/d/u/g/G`など）との衝突を先に確かめる。URL表示切替を`u`にして半ページ上と衝突し、`l`に変えた。

## vimの再現

- 2026-09-12 単語移動・段落移動のような「vimの挙動」は、自分で考えて書くとずれる（`{`/`}`で実際にずれた）。vimのソース（`search.c`の`fwd_word`/`bck_word`/`end_word`、`textobject.c`の`findpar`）の該当関数をそのまま移植する方が早くて正確。テストの期待値もvimで実際に確かめてから書く。

## 描画の判断

- 2026-09-12 画面幅より広い表は、列を単純に比例配分すると「folio」のような短い列まで4桁に削られて語が割れる。幅10桁以下の列は自然幅のまま確保し、広い列だけを比で縮める（`fit_columns`）。セル内の折り返しでは行頭禁則のぶら下げを切る（列がずれるため）。

## コードの分割

- 2026-09-12 描画・解析の中核は`src/lib.rs`側に置き、`folio view`（`src/main.rs`）はその薄い呼び出しにする。将来ファイラー本体を自作した時に、プレビュー欄が同じ中核をそのまま呼べるようにするため。ターミナル制御（画面の初期化・復帰・キー入力）はビューアー側に閉じ込め、中核からは触らせない。
