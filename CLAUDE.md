# CLAUDE.md — folio

このファイルはClaude Codeへのプロジェクト指示書。**会話開始時に必ず読む。**
共通の進行管理・Git・記録ルールはグローバル`~/.claude/CLAUDE.md`に従う。
ここには**このPJ固有のことだけ**を書く。

## このプロジェクト固有の前提
- 自分用の「文書を読む/編集する環境」をRustで作る。yaziのようなCUIで、マークダウンをVSCodeのプレビューに近い見やすさで表示することが出発点。
- 初手はファイラーを自作せず、**単体のビューアー`folio view <file>`をyaziから呼ぶ**形にする（ADRはPROGRESS.md）。
- 段階的に、対応形式の追加・表示のカスタマイズ・vim練習モードなどを足していく。
- **最大のリスク＝スコープ膨張**。やりたいことが多く、ファイラー自作・多形式対応・練習ツールへ同時に手を出すと何も完成しない。**1フェーズ1テーマ**で区切り、次フェーズの機能は`REQUIREMENTS.md`のバックログへ置くだけにする。

## REQUIREMENTS.mdの性質
- 機能仕様とフェーズごとのスコープ。フェーズを跨ぐ要望は「バックログ」に積むだけで、現フェーズには入れない。

## Git運用（グローバル§5の差分）
- リモート: `https://github.com/jeeee4/folio`（**private**）
- デフォルトブランチ: `main`。**低リスクの個人リポにつきmain直編集・直pushを許可**（グローバル§5.1のworktree必須の対象外）。
- コミット規約: 日本語の要約1行（50字目安）＋空行＋body（何を・なぜ・どう・影響範囲）
- push方針: グローバル§5の既定どおり、1作業ごとにcommit＋自動push

## 技術スタック / 開発ルール
- Rust edition 2024。ビルドは`cargo build`、テストは`cargo test`。
- **コミット前に`cargo fmt`と`cargo clippy -- -D warnings`を通す。**
- 依存を新しく足す時は**選定理由を`NOTES.md`に1行残す**（後から代替を検討できるように）。
- ユーザーは日本語でやり取りする。ドキュメントも日本語で書く。
