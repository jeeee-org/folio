# 要件 / スコープ

> 決めたこと（要求・方針・作業・予定）とスコープ、未決事項。**進み具合はPROGRESS.md**（計画の本体を二重に持たない）。決まった／変わったら即更新し、**片付いたら消す**（経緯はcheckpoints/に残る）。

最終更新: 2026-09-12

## 目的・スコープ

自分が読みやすい形で文書を眺め、そのまま編集に入れるCUI環境を作る。既存のページャやエディタでは、マークダウンが記号のまま表示されるか、逆にGUIを開かないと整形されて見えない。**ターミナルの中だけで、整形された文書とvimでの編集を往復できる**状態を作ることが狙い。

| 段階 | テーマ | 状態 |
|---|---|---|
| P1 | マークダウンビューアー単体＋yazi連携＋vimでの編集 | 完了（2026-09-12） |
| P2 | 読みやすさの作り込み（目次・検索・表・設定） | 進行中 |
| P3 | 対応形式の拡張（マークダウン以外の文書） | 未着手 |
| P4 | vimコマンド練習モード | 未着手 |
| P5 | ファイラー本体の自作（yazi連携から自立する） | 未着手 |

段階の記号`P1`〜`P5`のPはPhase（フェーズ）。記号だけでは中身が分からないので、**チャットや文書で呼ぶ時は「P2（読みやすさの作り込み）」のようにテーマを添える**（2026-09-12決定）。

## 要求・方針

### 全体に効く方針
- **最終的にはファイラー本体まで自作し、folio単体で完結させる**（2026-09-12決定・ADRはPROGRESS.md）。yaziへの依存はP1〜P4の間の足場で、置き換える前提で使う。ビューアーはどちらの道でも中核部品（自作ファイラーのプレビュー欄も同じ描画を使う）なので、**フェーズの順番は変えない**。
- 描画・解析の中核はライブラリ側（`src/lib.rs`）に置き、`folio view`はその薄い呼び出しにする（理由はNOTES.md）。
- 1フェーズ1テーマ。次フェーズの機能は「やること / バックログ」に積むだけで、現フェーズには入れない。

### P1（完了）: マークダウンビューアー単体＋yazi連携＋vimでの編集

`folio view <path>`で全記法（見出し・段落・強調・リスト・引用・コード・表・水平線・インラインコード・リンク）を描画し、vim風キーでスクロール、`e`でvimと往復、`folio render`のANSI出力をyaziのプレビュー欄とEnterに接続済み。生きている決定:
- 日本語の折り返しは表示幅（全角2桁）で行い、句読点・閉じ括弧の行頭禁則はぶら下げ
- リンクはVSCodeのプレビューと同じく**文字列だけを装飾してURLは出さない**
- `e`は**TUIを抜けて同じ端末で子プロセスとしてエディタを起動し、終了を待って再初期化**
- ファイル一覧・画像・他形式・設定はP1に含めなかった（それぞれP5・未決・P3・P2）

### P2の仕様（今のフェーズ）: 読みやすさの作り込み

狙い＝長い文書を**迷わず・探せて・目に合う**状態にする。P1で「読める」ようになったものを「読みやすい」に上げる。

含む:
1. **目次と見出しジャンプ** — `t`で目次を開閉（左に開く縦ペイン、幅は画面の1/4程度）。目次内は`j/k`で選び`Enter`で飛ぶ。現在位置の見出しを目次で強調。目次を閉じていても`]]`/`[[`で次/前の見出しへ
2. **ファイル内検索** — `/`で入力、`Enter`で確定、`n`/`N`で次/前、`Esc`で解除。ヒットを強調し、ステータス行に「3/12件」。大文字を含む時だけ大小を区別（smartcase）。検索対象は描画後の本文（記号を除いた、読んでいる文字列）
3. **画面幅より広い表** — セル内で折り返す。列幅は内容の長さの比で配分し、各列に最小幅を保証
4. **折り返しの最大幅と中央寄せ** — 広い端末で1行が長くなりすぎないよう最大幅（既定100桁）を設け、余りは左右に均等配分（リーダー表示）。狭い端末では従来どおり全幅
5. **設定ファイル** — `~/.config/folio/config.toml`（`$XDG_CONFIG_HOME`準拠）。無ければ今の既定で動く。項目: 配色（見出し・コード・リンク・引用など`Theme`の各色）、syntectのテーマ名、折り返しの最大幅、余白
6. **リンクのURL表示切替** — `u`でリンクの後ろにURLを薄く出す／消す

含まない（P2では作らない）:
- キーバインドの変更（設定ファイルは配色と幅だけ）
- リンクをキーで開く（端末とOSに依存し、費用対効果が薄い）
- 画像・mermaid・複数ファイル・他形式（それぞれ未決・未決・バックログ・P3）

P2の完了条件:
1. 長い文書（NOTES.mdや長いcheckpoint、200行超のもの）で、目次から任意の見出しへ飛べる
2. `/`で検索して`n`で次のヒットへ移動でき、ヒットが強調される
3. このリポのREQUIREMENTS.mdを幅60桁で開いても表が切れずに読める
4. 設定ファイルで配色と折り返し幅を変えられ、ファイルが無くても今と同じ既定で動く

## やること / バックログ

### P2（着手前の作業。実行中・完了の状態はPROGRESS.md側）
1. 折り返しの最大幅と中央寄せ（まずハードコード、2で設定に出す）
2. 設定ファイル（配色・syntectテーマ・最大幅・余白）
3. リンクのURL表示切替

使いながら拾った崩れ・違和感は、P2の中で随時直す。

### P3以降（順不同）
- キーバインドの変更（設定ファイル）
- リンクをキーで開く
- 対応形式の追加候補: CSV/TSV、JSON/YAML、プレーンテキスト、PDF、ソースコード
- vimコマンド練習モード（課題文書に対して操作を練習し、判定する）
- 複数ファイルを開いてタブ/バッファで切り替え
- ファイラー機能の自作（P5。作ること自体は決定済み、範囲は未定）

## 未決事項

- [ ] 画像をどう扱うか（yaziはKitty/sixelで表示できる。folio側でも対応するか、代替テキストのみか）
- [ ] mermaid等の図をどうするか（当面はコードブロックとして表示で十分か）
- [ ] 自作ファイラーが持つ範囲（ファイル操作をどこまで入れるか・画像プレビューの扱い）※P5で決める
