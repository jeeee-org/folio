# 要件 / スコープ

> 決めたこと（要求・方針・作業・予定）とスコープ、未決事項。**進み具合はPROGRESS.md**（計画の本体を二重に持たない）。決まった／変わったら即更新し、**片付いたら消す**（経緯はcheckpoints/に残る）。

最終更新: 2026-09-12

## 目的・スコープ

自分が読みやすい形で文書を眺め、そのまま編集に入れるCUI環境を作る。既存のページャやエディタでは、マークダウンが記号のまま表示されるか、逆にGUIを開かないと整形されて見えない。**ターミナルの中だけで、整形された文書とvimでの編集を往復できる**状態を作ることが狙い。

| 段階 | テーマ | 状態 |
|---|---|---|
| P1 | マークダウンビューアー単体＋yazi連携＋vimでの編集 | 完了（2026-09-12） |
| P2 | 読みやすさの作り込み（目次・検索・表・設定） | 完了（2026-09-12） |
| P3 | 対応形式の拡張（テキスト・ソースコード・CSV/TSV） | 完了（2026-09-12） |
| P4 | vimコマンド練習モード（移動の高速化） | 進行中 |
| P5 | ファイラー本体の自作（yazi連携から自立する） | 未着手 |

段階の記号`P1`〜`P5`のPはPhase（フェーズ）。記号だけでは中身が分からないので、**チャットや文書で呼ぶ時は「P2（読みやすさの作り込み）」のようにテーマを添える**（2026-09-12決定）。

## 要求・方針

### 全体に効く方針
- **最終的にはファイラー本体まで自作し、folio単体で完結させる**（2026-09-12決定・ADRはPROGRESS.md）。yaziへの依存はP1〜P4の間の足場で、置き換える前提で使う。ビューアーはどちらの道でも中核部品（自作ファイラーのプレビュー欄も同じ描画を使う）なので、**フェーズの順番は変えない**。
- 描画・解析の中核はライブラリ側（`src/lib.rs`）に置き、`folio view`はその薄い呼び出しにする（理由はNOTES.md）。
- 1フェーズ1テーマ。次フェーズの機能は「やること / バックログ」に積むだけで、現フェーズには入れない。
- **キー割当は3層**（2026-09-12決定）。①移動と検索は**vim準拠**（`j/k`・`gg`/`G`・`Ctrl-d/u/f/b`・`/`・`n/N`・`]]`/`[[`。将来は回数指定と`zt`/`zz`もここ）。②ページャ的操作は**lessと互換**（`d`/`u`・`Space`・`Enter`・`q`。vimと衝突しないので残す）。③アプリ操作（編集`e`・再読込`r`・目次`t`・URL表示`U`）は**vimの移動キーを避ける**。P4（vim練習モード）でビューアーと手が変わらないようにするため。

### P1（完了）: マークダウンビューアー単体＋yazi連携＋vimでの編集

`folio view <path>`で全記法（見出し・段落・強調・リスト・引用・コード・表・水平線・インラインコード・リンク）を描画し、vim風キーでスクロール、`e`でvimと往復、`folio render`のANSI出力をyaziのプレビュー欄とEnterに接続済み。生きている決定:
- 日本語の折り返しは表示幅（全角2桁）で行い、句読点・閉じ括弧の行頭禁則はぶら下げ
- リンクはVSCodeのプレビューと同じく**文字列だけを装飾してURLは出さない**
- `e`は**TUIを抜けて同じ端末で子プロセスとしてエディタを起動し、終了を待って再初期化**
- ファイル一覧・画像・他形式・設定はP1に含めなかった（それぞれP5・未決・P3・P2）

### P2（完了）: 読みやすさの作り込み

目次ペイン（`t`、`]]`/`[[`で見出し移動）・ファイル内検索（`/`、`n`/`N`、smartcase、描画後の本文が対象）・広い表のセル内折り返し・折り返しの最大幅（既定100桁）と中央寄せ・設定ファイル（`~/.config/folio/config.toml`、XDG準拠。配色・syntectテーマ・最大幅・余白）・URL表示切替（`U`）を実装済み。生きている決定:
- 目次は左に開く縦ペイン。幅が変わっても画面上端は「直前の見出し＋相対行数」で保つ
- 検索の対象は原文でなく描画後の本文（`**`や`#`にヒットさせない）
- 広い表は短い列（10桁以下）を保ち、広い列だけを比で縮めてセル内で折り返す
- 設定は配色と幅だけ。キーバインドの変更とリンクを開く操作はP3以降

### P3（完了）: 対応形式の拡張

拡張子で形式を判定（`--format`で上書き可）し、プレーンテキスト（幅で折り返し）・ソースコード（ファイル全体をハイライト付きコードブロック。JSON/YAML/TOML含む）・CSV/TSV（1行目を見出しにした表）を読める。yaziはCSV/TSVもEnterでfolioで開く。生きている決定:
- ハイライターが知らない拡張子はプレーンテキストに落とす
- 文字コードはUTF-8のみ（他は「未対応」と知らせる）
- PDF・行番号・JSON/YAMLの木構造表示はP4以降のバックログ

### P4の仕様（今のフェーズ）: vimコマンド練習モード

狙い＝**vimを高速に使えるようになること**。速く動くために有用な移動コマンドを、時間制限つきの出題で体に入れる。

含む:
1. **`folio practice [file] [--count N] [--time S]`** — 別サブコマンド。ファイルを省略すると内蔵の練習文（英文・コード・日本語の混在）。`--count`で問題数（既定10。短く済ませるにも長くやるにも）、`--time`で1問の制限時間（秒、既定10）
2. **vim風の画面** — 行番号・ブロックカーソル・下部のステータス行（NORMAL、行:桁）。folioが自前で描く（本物のvimではない）。**その上に枠付きの問題ボックスを重ねる**。端末に透過はないので、枠の外は本文をそのまま見せる
3. **移動コマンド（ノーマルモード、編集なし）** — `h j k l`、`w b e W B E`、`0 ^ $`、`gg G`と`{n}G`、`f F t T`と`; ,`、`{ }`、`%`、`H M L`、`Ctrl-d/u/f/b`、`/ ? n N`、回数指定（`3w` `5j`）。vimの挙動に合わせる（単語の境界・行末の扱い）
4. **出題** — 「N行目へ」「次の『x』へ」「3単語先へ」「行末へ」「次の段落へ」「対応する括弧へ」「『foo』を検索して移動」など、**型ごとに目標位置と模範解答（最短の打鍵）を持つ**。カーソルが制限時間内に目標に着けばクリア。クリア時と時間切れ時に模範解答を見せる
5. **採点** — クリア／時間切れ、所要時間、打鍵数（模範解答以下なら満点、超過分で減点）。セッションの合計点と型ごとの成績
6. **記録と振り返り** — セッション結果を`~/.local/share/folio/practice.jsonl`（XDG準拠）に追記。`folio practice --history`で過去の点数・正答率・型ごとの弱点を一覧
7. **出題の選び方** — ランダム。同じ問題の繰り返しも可。履歴で成績の悪い型を多めに出す（重み付け）

含まない（P4では作らない）:
- 編集コマンド（`d c y p x`、insertモード、text object、レジスタ、マクロ）
- `:`コマンド、複数ファイル、本物のvimとの連携（tmuxのpopup等はバックログ）
- 練習文の外部からの追加（当面は内蔵と`file`引数のみ）

P4の完了条件:
1. `folio practice`で1セッション（10問）を通せ、各問で制限時間・打鍵・クリア判定・模範解答の表示が働く
2. 結果が保存され、`folio practice --history`で過去の点数と型ごとの成績を見返せる
3. 出題に少なくとも「行番号ジャンプ・`f`/`t`・`w`/`b`・`0`/`^`/`$`・`gg`/`G`・`{`/`}`・`%`・検索」の型が含まれる
4. `--count`と`--time`で長さを変えられ、成績の悪い型が多めに出る

## やること / バックログ

### P4（着手前の作業。実行中・完了の状態はPROGRESS.md側）
1. vim風の画面とカーソル、移動コマンドのエンジン（回数指定含む）
2. 問題の型・生成・判定、問題ボックス、制限時間
3. 結果画面と履歴の保存・`--history`
4. 成績の悪い型を多めに出す重み付け

### P5以降（順不同）
- 練習モードで編集コマンド（`d c y p x`、text object）とinsertモード
- 本物のvimの上で出題する（tmuxのpopupなど）
- 練習文を外から足す仕組み
- キーバインドの変更（設定ファイル）
- リンクをキーで開く（`o`を予約）
- PDF（文字抽出。レイアウトは崩れる前提）
- ソースコードの行番号、JSON/YAMLの木構造表示
- vimコマンド練習モード（課題文書に対して操作を練習し、判定する）
- 複数ファイルを開いてタブ/バッファで切り替え
- ファイラー機能の自作（P5。作ること自体は決定済み、範囲は未定）

## 未決事項

- [ ] 画像をどう扱うか（yaziはKitty/sixelで表示できる。folio側でも対応するか、代替テキストのみか）
- [ ] mermaid等の図をどうするか（当面はコードブロックとして表示で十分か）
- [ ] 練習モードで日本語の行に単語移動（`w`/`b`）の問題を出すか（vimのCJKでの単語境界は分かりにくい。当面は英字の行だけに出題）
- [ ] 自作ファイラーが持つ範囲（ファイル操作をどこまで入れるか・画像プレビューの扱い）※P5で決める
