# 要件 / スコープ

> 決めたこと（要求・方針・作業・予定）とスコープ、未決事項。**進み具合はPROGRESS.md**（計画の本体を二重に持たない）。決まった／変わったら即更新し、**片付いたら消す**（経緯はcheckpoints/に残る）。

最終更新: 2026-09-13

## 目的・スコープ

自分が読みやすい形で文書を眺め、そのまま編集に入れるCUI環境を作る。既存のページャやエディタでは、マークダウンが記号のまま表示されるか、逆にGUIを開かないと整形されて見えない。**ターミナルの中だけで、整形された文書とvimでの編集を往復できる**状態を作ることが狙い。

| 段階 | テーマ | 状態 |
|---|---|---|
| P1 | マークダウンビューアー単体＋yazi連携＋vimでの編集 | 完了（2026-09-12） |
| P2 | 読みやすさの作り込み（目次・検索・表・設定） | 完了（2026-09-12） |
| P3 | 対応形式の拡張（テキスト・ソースコード・CSV/TSV） | 完了（2026-09-12） |
| P4 | vimコマンド練習モード（移動の高速化） | 完了（2026-09-12） |
| P5 | ファイラー本体の自作（第一歩: 一覧・移動・プレビュー） | 完了（2026-09-12） |
| P6 | ファイラーのファイル操作（安全な範囲: 作成・改名・ごみ箱） | 完了（2026-09-13） |
| P7 | コピー・移動とyaziの置き換え | 完了（2026-09-13） |

段階の記号`P1`〜`P5`のPはPhase（フェーズ）。記号だけでは中身が分からないので、**チャットや文書で呼ぶ時は「P2（読みやすさの作り込み）」のようにテーマを添える**（2026-09-12決定）。

## 要求・方針

### 全体に効く方針
- 2026-09-09 **yaziから呼ぶ単体ビューアーから始める**（プラグイン・fork・ファイラー自作を退けた理由はNOTES.md「方針の理由」）。
- 2026-09-12 **最終的にはファイラー本体まで自作し、folio単体で完結させる**。yaziへの依存は足場で、置き換える前提で使う。ビューアーはどちらの道でも中核部品（自作ファイラーのプレビュー欄も同じ描画を使う）なので、**フェーズの順番は変えない**（理由はNOTES.md「方針の理由」）。
- 2026-09-12 描画・解析の中核はライブラリ側（`src/lib.rs`）に置き、`folio view`はその薄い呼び出しにする（理由はNOTES.md「コードの分割」）。
- 2026-09-09 個人リポにつき**main直編集・直push**（worktree必須の対象外。理由はNOTES.md「方針の理由」）。
- 2026-09-09 1フェーズ1テーマ。次フェーズの機能は「やること / バックログ」に積むだけで、現フェーズには入れない。
- 2026-09-12 **キー割当は3層**。①移動と検索は**vim準拠**（`j/k`・`gg`/`G`・`Ctrl-d/u/f/b`・`/`・`n/N`・`]]`/`[[`。将来は回数指定と`zt`/`zz`もここ）。②ページャ的操作は**lessと互換**（`d`/`u`・`Space`・`Enter`・`q`。vimと衝突しないので残す）。③アプリ操作（編集`e`・再読込`r`・目次`t`・URL表示`U`）は**vimの移動キーを避ける**。P4（vim練習モード）でビューアーと手が変わらないようにするため。

### P1（完了）: マークダウンビューアー単体＋yazi連携＋vimでの編集

`folio view <path>`で全記法（見出し・段落・強調・リスト・引用・コード・表・水平線・インラインコード・リンク）を描画し、vim風キーでスクロール、`e`でvimと往復、`folio render`のANSI出力をyaziのプレビュー欄とEnterに接続済み。生きている決定:
- 日本語の折り返しは表示幅（全角2桁）で行い、句読点・閉じ括弧の行頭禁則はぶら下げ
- リンクはVSCodeのプレビューと同じく**文字列だけを装飾してURLは出さない**
- `e`は**TUIを抜けて同じ端末で子プロセスとしてエディタを起動し、終了を待って再初期化**
- ファイル一覧・画像・他形式・設定はP1に含めなかった（それぞれP5・未決・P3・P2）

### P2（完了）: 読みやすさの作り込み

目次ペイン（`t`、`]]`/`[[`で見出し移動）・ファイル内検索（`/`、`n`/`N`、smartcase、描画後の本文が対象）・広い表のセル内折り返し・折り返しの最大幅（既定100桁）と中央寄せ・設定ファイル（`~/.config/folio/config.toml`、XDG準拠。配色・syntectテーマ・最大幅・余白）・URL表示切替（`U`）を実装済み。生きている決定:
- 目次は左に開く縦ペイン。幅が変わっても画面上端は「直前の見出し＋相対行数」で保つ
- 検索の対象は原文でなく描画後の本文（`**`や`#`にヒットさせない）
- 広い表は短い列（10桁以下）を保ち、広い列だけを比で縮めてセル内で折り返す
- 設定は配色と幅だけ。キーバインドの変更とリンクを開く操作はP3以降

### P3（完了）: 対応形式の拡張

拡張子で形式を判定（`--format`で上書き可）し、プレーンテキスト（幅で折り返し）・ソースコード（ファイル全体をハイライト付きコードブロック。JSON/YAML/TOML含む）・CSV/TSV（1行目を見出しにした表）を読める。yaziはCSV/TSVもEnterでfolioで開く。生きている決定:
- ハイライターが知らない拡張子はプレーンテキストに落とす
- 文字コードはUTF-8のみ（他は「未対応」と知らせる）
- PDF・行番号・JSON/YAMLの木構造表示はP4以降のバックログ

### P4（完了）: vimコマンド練習モード

`folio practice [file] [--count N] [--time S]`。vim風の画面の上に問題ボックスを重ね、制限時間内に目標へ移動できたらクリア。16種の型（行番号・`f`/`t`・`w`/`b`/`e`・`$`/`0`/`^`・`gg`/`G`・`{`/`}`・`%`・検索・`H`/`M`/`L`）、採点（クリア100点、余分な打鍵1つにつき10点減、時間切れ0点）、履歴（`~/.local/share/folio/practice.jsonl`）と`--history`（型ごとの弱点）、成績の悪い型を多めに出す重み付け。生きている決定:
- **判定は既定で厳格**（模範解答と同等の別解以外のキーを押した時点で不正解にし、打ったキーと答えを見せる）。`--loose`で緩い判定（着けばクリア、余分な打鍵で減点）。初見は`--show`（模範解答を見ながら打つ）、覚える段階は厳格、慣れたら緩いで総合力、という3段階（2026-09-12決定）
- 移動のみ。編集コマンドと本物のvimとの連携はP5以降
- 目標位置は模範解答をエンジンで動かして求める（判定がvimの挙動と一致する）
- 目標位置は画面に出さない（指示から自分で読む）
- 単語系の出題は英字の行だけ（日本語の単語境界は未決）

### P5（完了）: ファイラー本体の自作（第一歩）

`folio browse [dir] [--cwd-file f]`。2ペイン（一覧・プレビュー）。`j/k/gg/G/d/u`・`h`親・`l`/`Enter`入る・ファイルで`Enter`はビューアー全画面→`q`で戻る・`e`でvim・`.`隠し切替・`J/K`プレビュー・`q`終了。ディレクトリ先・自然順。1MB超と非UTF-8は「プレビューなし」。生きている決定:
- **yaziは併用**。ファイル操作（コピー・移動・削除・改名・作成）はfolioに無く、yaziに任せる。置き換えの判断はP6以降
- プレビューはビューアーと同じ中核（format / render）を使う。ターミナルの初期化は外側で1回、ビューアーは借りて動かす（NOTES.md「TUIの入れ子」）
- 2ペイン。3ペイン（親の文脈）はバックログ

### P6（完了）: ファイラーのファイル操作（安全な範囲）

`a`作成（末尾`/`でディレクトリ、途中も作る、既存名は拒む）・`r`改名（既存名への上書きは拒む）・`d`ごみ箱（`y`で確認、XDG Trash、同名は番号、別ファイルシステムは断る）。入力は下部の1行。操作後は一覧を読み直し、作成・改名は対象に、削除は同じ位置にカーソル。生きている決定:
- 2026-09-13 ファイラーのキーはyaziの慣例（`a` `r` `d`、将来`y` `x` `p`）を優先し、半ページは`Ctrl-d/u`、プレビューは`J/K`、読み直しは`R`。キー方針3層の例外
- 2026-09-13 失って困る操作（完全削除・上書き・複数選択）は入れない。コピー・移動・別ファイルシステムのごみ箱移動・undo・復元はP7以降

### P7（完了）: コピー・移動とyaziの置き換え

`y`コピー元・`x`移動元・`p`貼り付け（1件ずつ、ディレクトリは中身ごと）。同名は`y/N`で聞いてから上書き（2026-09-13決定: 上書きが既定、確認は1回）。別ファイルシステムへの移動とごみ箱はコピー＋削除。`o`で外部アプリ（`wslview`→`xdg-open`）。`Esc`で覚えた項目を忘れる。生きている決定:
- 2026-09-13 日常の入口は`folio browse`（シェル関数`fb`）。yaziの設定は消さずに残し、戻れるようにする
- 複数選択・一括操作・undo・復元・進捗表示はP8以降

## やること / バックログ

### yaziにあってfolioに無いもの（2026-09-13時点。yaziを消す前に、日常で困ったものからP8に入れる）
- 複数選択と一括操作（1件ずつしかできない）
- 画像プレビュー（画像は「プレビューなし（UTF-8ではない）」。yaziはKitty/sixelで表示）
- フィルタ・検索・ジャンプ（zoxide連携など）
- ソート順の切替、サイズ・更新日時の表示（今は名前の自然順だけ）
- 3ペイン（親ディレクトリの文脈）
- タブ、ブックマーク、シェルコマンドの実行
- 完全削除、undo、ごみ箱からの復元、アーカイブ展開
- 別ファイルシステムへの移動・ごみ箱（実装はあるが実機で未確認）

### P8以降（順不同。P8のスコープはこれから切る）
- 上記の「yaziにあってfolioに無いもの」のうち、使っていて困ったもの
- 大きなコピーの進捗表示
- 練習モードで編集コマンド（`d c y p x`、text object）とinsertモード
- 本物のvimの上で出題する（tmuxのpopupなど）
- 練習文を外から足す仕組み
- キーバインドの変更（設定ファイル）
- リンクをキーで開く（`o`を予約）
- PDF（文字抽出。レイアウトは崩れる前提）
- ソースコードの行番号、JSON/YAMLの木構造表示
- 複数ファイルを開いてタブ/バッファで切り替え

## 未決事項

- [ ] 画像をどう扱うか（yaziはKitty/sixelで表示できる。folio側でも対応するか、代替テキストのみか）
- [ ] mermaid等の図をどうするか（当面はコードブロックとして表示で十分か）
- [ ] 練習モードで日本語の行に単語移動（`w`/`b`）の問題を出すか（vimのCJKでの単語境界は分かりにくい。当面は英字の行だけに出題）
- [ ] 自作ファイラーが持つ範囲（ファイル操作をどこまで入れるか・画像プレビューの扱い）※P5で決める
