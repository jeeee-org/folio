# 要件 / スコープ

> 決めたこと（要求・方針・作業・予定）とスコープ、未決事項。**進み具合はPROGRESS.md**（計画の本体を二重に持たない）。決まった／変わったら即更新し、**片付いたら消す**（経緯はcheckpoints/に残る）。

最終更新: 2026-09-12

## 目的・スコープ

自分が読みやすい形で文書を眺め、そのまま編集に入れるCUI環境を作る。既存のページャやエディタでは、マークダウンが記号のまま表示されるか、逆にGUIを開かないと整形されて見えない。**ターミナルの中だけで、整形された文書とvimでの編集を往復できる**状態を作ることが狙い。

| 段階 | テーマ | 状態 |
|---|---|---|
| P1 | マークダウンビューアー単体＋yazi連携＋vimでの編集 | 進行中 |
| P2 | 読みやすさの作り込み（目次・検索・テーマ設定） | 未着手 |
| P3 | 対応形式の拡張（マークダウン以外の文書） | 未着手 |
| P4 | vimコマンド練習モード | 未着手 |
| P5 | ファイラー本体の自作（yazi連携から自立する） | 未着手 |

## 要求・方針

### 全体に効く方針
- **最終的にはファイラー本体まで自作し、folio単体で完結させる**（2026-09-12決定・ADRはPROGRESS.md）。yaziへの依存はP1〜P4の間の足場で、置き換える前提で使う。ビューアーはどちらの道でも中核部品（自作ファイラーのプレビュー欄も同じ描画を使う）なので、**フェーズの順番は変えない**。
- 描画・解析の中核はライブラリ側（`src/lib.rs`）に置き、`folio view`はその薄い呼び出しにする（理由はNOTES.md）。
- 1フェーズ1テーマ。次フェーズの機能は「やること / バックログ」に積むだけで、現フェーズには入れない。

### P1の仕様（今のフェーズ）

作るもの＝`folio view <path>`。マークダウンファイルを整形してターミナルに表示するTUI。

含む:
- 記法の描画: 見出し・段落・強調・箇条書き/番号付きリスト・引用・コードブロック・表・水平線・インラインコード・リンク（URLの見せ方は未決）
- コードブロックの言語別シンタックスハイライト
- 上下スクロール（vim風のキー: `j` `k` `d` `u` `g` `G`）
- `q`で終了
- `e`で`$EDITOR`（vim）を起動し、閉じたらビューアーに戻って再読み込み
- yaziからの呼び出し設定（Enterで開くopener登録、およびプレビュー欄での利用）

含まない（P1では作らない）:
- ファイル一覧・ディレクトリ移動（yaziに任せる）
- 画像・図の描画
- マークダウン以外の形式
- 設定ファイルによるカスタマイズ（色は当面ハードコードでよい）

P1の完了条件:
1. 手元の実ファイル（このリポのREQUIREMENTS.md自身など、表やコードブロックを含むもの）を`folio view`で崩れずに読める
2. yaziでマークダウンにEnterを押すとfolioが開く
3. ビューアーから`e`でvimに入り、保存して抜けると表示が更新される

## やること / バックログ

### P1（着手前の作業。実行中・完了の状態はPROGRESS.md側）
1. 実装を止める未決事項を決める（リンクの見せ方、`e`でvimへ抜ける方式）
2. 依存クレートを選ぶ（TUI描画、マークダウン解析、シンタックスハイライト）。選定理由をNOTES.mdへ
3. `folio view <path>`の最小実装: 見出し・段落・リスト・コードブロックの描画とスクロール
4. 残りの記法（強調・引用・表・水平線・インラインコード・リンク）とシンタックスハイライト
5. `e`でvimを起動して戻る導線と再読み込み
6. yaziのopener登録とプレビュー欄での利用

### P2以降（順不同）
- 目次(TOC)ペインと見出しジャンプ
- ファイル内検索（`/`）
- 設定ファイル（TOML想定）によるテーマ・キーバインドの変更
- 折り返し幅・余白の調整、リーダー的な中央寄せ表示
- 対応形式の追加候補: CSV/TSV、JSON/YAML、プレーンテキスト、PDF、ソースコード
- vimコマンド練習モード（課題文書に対して操作を練習し、判定する）
- 複数ファイルを開いてタブ/バッファで切り替え
- ファイラー機能の自作（P5。作ること自体は決定済み、範囲は未定）

## 未決事項

- [ ] リンクとURLの見せ方（本文中に出す／脚注のように末尾へ集める／キーで開く）
- [ ] 画像をどう扱うか（yaziはKitty/sixelで表示できる。folio側でも対応するか、代替テキストのみか）
- [ ] mermaid等の図をどうするか（当面はコードブロックとして表示で十分か）
- [ ] `e`で編集に入る時の起動方法（TUIを抜けてvimを前面に出す／別の方式）
- [ ] yaziのプレビュー欄で使う場合、スクロール制御をyaziとどう分担するか
- [ ] 設定ファイルの置き場所（`~/.config/folio/`想定）
- [ ] 自作ファイラーが持つ範囲（ファイル操作をどこまで入れるか・画像プレビューの扱い）※P5で決める
