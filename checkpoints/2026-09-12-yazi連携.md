# 2026-09-12 yazi連携

`folio render`（ANSI出力）を足し、yaziのプレビュー欄とEnterでの起動をfolioに切り替えた。これでP1の完了条件3つが揃った。

## やったこと

- `src/ansi.rs`を新設。ratatuiの`Line`をSGR付き文字列にする（前景・背景は基本16色／256色／RGB、太字・薄字・斜体・下線・打ち消し）。テスト3件。
- `folio render <path> [--width N]`を追加。TUIを開かず、整形結果を標準出力へ流す。幅は引数→`$COLUMNS`→80。受け側が閉じたら静かに終える。
- `~/.config/yazi/yazi.toml`を更新（バックアップ`yazi.toml.bak.20260912-153540`）。glowによるプレビューを`piper -- folio render --width "$w" "$1"`に置き換え（glow行はコメントで残した）。openerに`folio`（`block = true`）を足し、`[open] prepend_rules`で`*.md`を`["folio", "edit"]`に。
- `cargo install --path .`で`~/.cargo/bin/folio`に入れた。
- READMEに使い方とyazi設定の断片を書いた。

## 確認したこと

tmuxでyaziを起動し、README.mdにカーソルを置いた状態でプレビュー欄にfolioの整形結果（見出しの下線・リスト）が出ること、Enterでfolioが前面に開くこと、`q`でyaziに戻ることを確認。

## ハマり

- 最初`[open]`のルールを`name = "*.md"`で書いたら黙ってvimが開いた。yazi 26.xでは`url`。yaziの既定設定はバイナリに埋め込まれており`strings`で読めた。

## P1の完了条件の状態

1. 実ファイル（REQUIREMENTS.md）が崩れず読める — 達成（表の幅超過は既知の未決）
2. yaziでEnterを押すとfolioが開く — 達成
3. `e`でvimに入り保存して抜けると表示が更新される — 達成（前の作業）
