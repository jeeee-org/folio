# 2026-09-13 コピーと移動

P7（コピー・移動とyaziの置き換え）の実装と締め。

## やったこと

- `fileops.rs`: `copy_into`（中身ごと再帰。シンボリックリンクはリンクのまま）、`move_into`（`rename`→`CrossesDevices`ならコピー＋削除。コピー完了後に元を消し、失敗したら行き先を消して戻す）、`prepare_target`（自分自身・自分の中への操作を拒み、同名は`overwrite`に従って先に消す）、ごみ箱の別FS対応、`open_external`（`wslview`→`xdg-open`。出力は捨てて待たない）。テスト1件追加。
- `browser.rs`: `y`/`x`で覚え（ステータス行に「[コピー元: 名前]」）、`p`で貼り付け。同名は「上書きしますか (y/N)」を挟む。移動後は覚えた項目を忘れる。`Esc`は覚えた項目があれば忘れ、無ければ終了。`o`で外部アプリ。案内を更新。
- README: キー一覧にコピー・移動・外部アプリを追加し、「yaziから置き換える」の節（シェル関数`fb`、yaziの設定は残す）を書いた。

## 確認したこと

サンドボックスで、`y`→別ディレクトリで`p`→同名の確認→`y`で上書き（中身が置き換わる）、ディレクトリを`x`→`p`で中身ごと移動、移動後の`p`は「先に覚えてください」。

別ファイルシステムの経路は、このPCでは`/tmp`と`/`が同じデバイスで再現できず未確認（NOTES.md）。

## P7の完了条件の状態

1. コピー・移動・上書き確認 — 達成
2. 別FSのコピー＋削除 — コード上は実装、実機では未確認
3. `o`で外部アプリ — 実装（tmuxでは外部アプリの起動先が無いので未確認）
4. READMEの置き換え手順 — 達成

## ハマり

- rustfmt後の文字列一致の置換がまた外れた（3回目）。今回は最後の1箇所だけ正規表現にして通した。**置換スクリプトは各置換の直前に対象を表示して確かめる**か、関数ごと範囲で差し替える。NOTES.mdに既に書いてある学びなので、次は最初からそうする。
