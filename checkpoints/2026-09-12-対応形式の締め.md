# 2026-09-12 対応形式の締め

P3（対応形式の拡張）の3件目（yazi設定）と、完了条件の確認。

## やったこと

- `~/.config/yazi/yazi.toml`の`[open] prepend_rules`に`*.{csv,tsv}`→folioを追加。プレビュー欄はyazi標準のコード表示のまま。`.txt`やソースコードのEnterは変えていない（今はvimが開く。folioにしたければ同じ形でルールを足す）。
- READMEの概要・使い方・yazi設定を更新。
- `cargo install`で再インストール。

## 完了条件の確認

1. `folio view src/main.rs`が色付き — 確認済み（実装の作業）
2. 引用符・改行を含むセルのCSVが表として崩れない — 確認済み
3. 拡張子なしのテキストが幅で折り返される — 確認済み
4. `--format`で上書きできる — 確認済み

tmuxでyaziから`t.csv`にEnterを押し、folioの表が開くことを確認。

## 反映

- `REQUIREMENTS.md`: P3の仕様を完了の要約に畳み、段階の表を完了に。
- `PROGRESS.md`: 現在のフェーズと次の一手をP4（vimコマンド練習モード）のスコープ切りに。完了欄はP3を1行に畳んだ。
