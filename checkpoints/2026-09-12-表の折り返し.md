# 2026-09-12 表の折り返し

P2（読みやすさの作り込み）の3件目。画面幅より広い表をセル内で折り返す。

## やったこと

- `fit_columns`を新設。列の自然幅の合計が幅に収まればそのまま。溢れる時は、幅10桁以下の短い列は自然幅のまま確保し、残りを広い列に自然幅の比で配分（各列最低4桁）。切り捨てのずれは広い列の間で1桁ずつ調整して合計を幅に合わせる。短い列だけでも入らない時は全列を比例配分に落とし、最小幅すら入らない時は均等にしてはみ出しを諦める。
- 表の各行を、セルごとに`wrap_inlines_opts`（ぶら下げ無し）で折り返し、最も行数の多いセルに合わせて描画行を作る。空いたセルは空白で埋め、揃えは各行に効かせる。
- `wrap_inlines`にぶら下げの可否を持たせた（表のセルは列がずれるので禁止）。
- 最初の版は単純比例で「folio」列が4桁に削られて語が割れたので、短い列を保つ方式に直した。
- テスト2件（幅30で日本語の表が収まり空セルが空白で埋まる／`fit_columns`の3つの場面）。

## 確認したこと

tmuxで幅60にして`REQUIREMENTS.md`の段階の表を表示。「段階」列は4桁のまま、「テーマ」列が2行に折り返され、罫線と縦線が揃っていることを確認。P2の完了条件3（幅60で表が切れない）を満たした。
