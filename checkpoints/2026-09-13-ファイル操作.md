# 2026-09-13 ファイル操作

P6（ファイラーのファイル操作・安全な範囲）の実装と締め。

## やったこと

- `src/fileops.rs`を新設。`create`（末尾`/`でディレクトリ、途中も作る、`File::create_new`で既存なら失敗、絶対パスと`..`は拒む）、`rename`（同じディレクトリ内のみ、既存名は拒む）、`trash`/`trash_into`（XDG Trash。infoを先に書いてから移す。同名は`files`と`info`の両方で空く番号。ルート・ホーム・ごみ箱を含むディレクトリは断る。別ファイルシステムは`CrossesDevices`で判定して断る）。テスト5件。
- `browser.rs`に下部1行の入力・確認（`Prompt`）を追加。`a`作成・`r`改名（今の名前入り）・`d`ごみ箱（`y`だけが実行）。`Esc`/`Ctrl-c`で中止。操作後は一覧を読み直し、作成・改名は対象へ、削除は同じ位置にカーソル。
- キーの整理: 半ページ`Ctrl-d/u`、プレビュー`J/K`、読み直し`R`（yaziに合わせた）。
- ステータス行の左側を幅で切る（長いメッセージが右の案内に食い込んでいた）。ごみ箱の確認は案内を「y:実行 それ以外:中止」に。
- README・要件・NOTES（XDG Trashの順序、CrossesDevices、create_new）を更新。

## 確認したこと

`target/`配下のサンドボックスと`XDG_DATA_HOME`で、`a`→`new.md`作成とカーソル移動、`a`→`sub/`作成、同名の拒否、`r`で`renamed.md`に改名、`d`→`n`で中止、`d`→`y`でごみ箱へ移り`Trash/files/renamed.md`と`Trash/info/renamed.md.trashinfo`（Pathと日時）が揃うこと、削除後にカーソルが同じ位置（末尾なら1つ上）に残ること。長いメッセージが`…`で切れること。

## P6の完了条件の状態

1〜4すべて達成（別ファイルシステムの拒否はコード上の判定。tmuxでは未確認）。
