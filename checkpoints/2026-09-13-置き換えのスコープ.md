# 2026-09-13 置き換えのスコープ

P7（コピー・移動とyaziの置き換え）のスコープを切った。

## 伝えられた決定

1. コピー・移動で同名がある時は**上書き**
2. yaziは**もう置き換えてよい**

## 決めたこと

- 上書きは既定にするが、実行前に`y/N`で1回聞く（ごみ箱と同じ流儀。聞かずに上書きしない）。
- 置き換えの受け皿として、`o`で外部アプリに渡す（画像・PDF）と、別ファイルシステムへの移動・ごみ箱（コピー＋削除）まで入れる。
- yaziの設定は消さずに残す（戻れるように）。日常の入口はシェル関数で`folio browse`に。
- 複数選択・undo・進捗表示はP8以降。
